sintaxis

¿Cuál de estas afirmaciones es correcta?

a.  Los atributos están dentro de las etiquetas de apertura, que forman
    parte de los elementos.

b.  Las etiquetas están dentro de los atributos, que pueden tener
    elementos.

c.  Los elementos tienen etiquetas de apertura. A menos que sean de tipo
    void, entonces no.

d.  Los elementos tienen atributos. A menos que sean de tipo void,
    entonces no.
